# ImageFlow 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值

# Google AdSense 配置
# 从 Google AdSense 控制台获取您的发布商 ID
# 格式: ca-pub-XXXXXXXXXXXXXXXX
# 示例: ca-pub-1460768247956454
VITE_GOOGLE_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX

# Google Analytics 配置
# 从 Google Analytics 控制台获取您的测量 ID
# 格式: G-XXXXXXXXXX
VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Google Search Console 验证代码
# 从 Google Search Console 获取网站验证代码
VITE_GOOGLE_SEARCH_CONSOLE_VERIFICATION=your_verification_code

# Supabase配置
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# 功能开关
VITE_ENABLE_SERVER_AVIF=true
VITE_MAX_FILE_SIZE=209715200
VITE_MAX_FILE_COUNT=50

# API配置
VITE_API_BASE_URL=http://localhost:3001

# 开发环境配置
NODE_ENV=development
VITE_API_URL=http://localhost:3002
PORT=3000
API_PORT=3002

# 应用配置
# 生产环境域名
VITE_APP_URL=https://imageflow.app

# API 配置（如果需要）
VITE_API_BASE_URL=https://api.imageflow.app

# 功能开关
# 是否启用广告（生产环境建议为 true）
VITE_ENABLE_ADS=true

# 是否启用分析（生产环境建议为 true）
VITE_ENABLE_ANALYTICS=true

# 是否启用性能监控（生产环境建议为 true）
VITE_ENABLE_PERFORMANCE_MONITORING=true

# 开发环境配置
# 是否启用调试模式
VITE_DEBUG_MODE=false

# 配置说明:
# 1. Google AdSense ID: 在 AdSense 控制台 -> 账户 -> 账户信息中找到
# 2. Google Analytics ID: 在 GA4 控制台 -> 管理 -> 数据流中找到
# 3. Search Console 验证码: 在 GSC -> 设置 -> 所有权验证中获取
# 4. 所有以 VITE_ 开头的变量都会在构建时注入到客户端代码中
# 5. 请确保不要将包含敏感信息的 .env.local 文件提交到版本控制系统